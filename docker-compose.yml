version: '3'
services:
  budgeting:
    image: budgeting:latest
    ports:
      - '4200:3000'
    volumes:
      - ./private:/usr/src/private
    environment:
      - HOST=0.0.0.0
      - REPOSITORY_PATH=/usr/src/private/
